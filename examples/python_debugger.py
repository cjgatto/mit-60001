# Press F5 to run this file in the debugger.

# The debugger will automatically stop when it hits an exception.

# Hover over the `some_number` variable to see its value.

# Set a breakpoint at `some_number` by clicking to the left of the line numbers, rerun.

import random

some_number = random.random()

bad_math = some_number / 0